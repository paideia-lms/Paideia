{
	"name": "paideia-monorepo",
	"private": true,
	"type": "module",
	"workspaces": ["apps/*"],
	"scripts": {
		"build": "bun run --cwd apps/paideia build",
		"lefthook:install": "lefthook install"
	}
}
