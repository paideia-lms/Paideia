# EXAMPLE USAGE:
#
#   Refer for explanation to following link:
#   https://lefthook.dev/configuration/
#
# pre-push:
#   jobs:
#     - name: packages audit
#       tags:
#         - frontend
#         - security
#       run: yarn audit
#
#     - name: gems audit
#       tags:
#         - backend
#         - security
#       run: bundle audit
#
# pre-commit:
#   parallel: true
#   jobs:
#     - run: yarn eslint {staged_files}
#       glob: "*.{js,ts,jsx,tsx}"
#
#     - name: rubocop
#       glob: "*.rb"
#       exclude:
#         - config/application.rb
#         - config/routes.rb
#       run: bundle exec rubocop --force-exclusion {all_files}
#
#     - name: govet
#       files: git ls-files -m
#       glob: "*.go"
#       run: go vet {files}
#
#     - script: "hello.js"
#       runner: node
#
#     - script: "hello.go"
#       runner: go run

pre-commit:
  parallel: true
  jobs:
    - name: check-changelog
      run: |
        if [ -z "$(git diff --cached --name-only --diff-filter=ACMR | grep '^changelogs/.*\.md$')" ]; then
          echo "Error: At least one changelog file must be modified or added in changelogs/ directory"
          exit 1
        fi
    - name: typecheck
      run: bun typecheck
      stage_fixed: true
    - name: lint
      run: bun lint --diagnostic-level error
      stage_fixed: true
    - name: lint-project
      run: bun lint:project
      stage_fixed: true
    # - name: test
    #   run: bun test 
    #   stage_fixed: true
