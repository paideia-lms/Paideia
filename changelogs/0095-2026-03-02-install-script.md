# Install Script

- Added `scripts/install.sh` for easy installation of the latest release from GitHub
- Supports Linux (amd64/arm64), macOS arm64, and Windows
- Automatically detects OS and architecture
- Installs to `~/.local/bin`, `~/bin`, or `/usr/local/bin`
- Updates PATH in shell configuration files
- No authentication required for public repository
